manage_count_cot
subject : Not interpret unclosed quotes
Donc modification du code et on enleve la condition si on ne trouve pas justement
(je me suis pris la tete pour rien...)

Meilleur gestion des symbols tels que hello|cat ou hello |cat ou hello| cat

A FAIRE :
Gerer la memoire des strings en faisant attention aux variables d'environment et le $?
donc remplacer i=4 -> $USER en i=7 -> Hassmou
Meme chose pour le $?

J'avais pas mal avancer, j'avais fais le lexer, la tokenisation 
puis le parse pour enfin le mettre dans une structure cmd, qui lui est aussi avec la struct env dans la struct shell. 
Je n'avais pas encore toucher aux varables d'environnement ce qui ma fallut que je reprenne le projet 
depuis le debut du lexer ce qui m'a valu pas mal de temps et je me suis rendu compter qu'il fallait que je change 
toute la structure de mes programmes pour faire en sorte que $USER soit Hassmou 
et donc supprimer le compteur de caractere pour $USER et enfin ajouter celui de Hassmou, pour avoir la taille final afin d'allouer de la memoire. 
En faisant ca, je me suis aussi rendu compte que certaines commande comme "hello | cat", 
je devais aussi gerer le "hello|cat" ainsi que "hello| cat" et ainsi de suite donc 
j'ai du rechanger mon lexer pour avoir une bonne taille pour mon char **.

Feat : ajout d'un exit status dans la struct t_shell 
Et initialisation de cette structe avant la boucle infinie.

Feat : variable supp deplacer index_in_cot -> manage_lex
donc changement du calcule de fin dans index_in_cot 'add = (i - add) + value_env;' 

about_env
Fix : MAJ sur les fonctions en faisant appel aux autres fonctions isdigit ect,
remplacement des parametres t_env *env par t_shell *shell
Feat : ajout d'une condition si $? alors i += 2 et sup += 2. return strlen de ce exit_code 


about_env
Feat : Ajout de la fonction qui compter le nombre de caractere dans le shell->exit_code
about create_tab
Fix : Meilleur gestion du manage_count qui comptais a l'infinie si ""$?$USER" | ahsh'$USER'"$USER" AHAH"
donc si il y avait '' puis directement "". Je n'avais pas gerer le cas ou il y aurait les 2 qui 
s'enchainent vu qu'il y avait l'index qui faisait +1 si un quote etait finit.ainsi il ne voyait pas l'autre qui apparaissait.

about create_tab and browse_line
Fix : Suppression du fichier browse_line -> Grosse restructuration du lexer et de la tokenisation 
= Maintenant on arrete de compter le nbr de caractere pour l'expansion, 
juste on creer un tableau de string puis compter le nombre de caractere apres de la tokenisation lors de l'expansion
donc suppression de la fonction index_in_cote (qui comptais les $) pour maintenant utiliser manage_count_cot qui de vient -> index_count_cote

about manage_lex
Fix : ajout d'une condition qui permet de savoir si Hello<World et donc de creer 3 string 
pour separer la redir ou le pipe

hello<world

about get_value
Fix : Ajout d'une condition lorsqu'on trouve que la strncmp head->key est good, on verifie si head->value est vrai pour eviter 
de retourner une valeur NULL et que je puisee utiliser strlen(get_value(key)), pour eviter un segfault.

about expand
Feat : Ajout d'une fonctionnalite qui permet de compter le nombre de caractere 
a allouer pour la string final sans les quot et en changeant les variables d'environment
WIP

about expand
Feat : Ajout d'une fonctionnalite qui permet de copier tout les caracteres pour la string final sans
quotes et sans $
WIP
Feat : Creation d'un nouveau dossier qui Expansions avec les fichieirs dedans correspondant

about expand
Feat : expansion.h
Feat : Creation d'une nouvelle structure t_exp *exp (expand) avec des variables i, new_size et char * final_ str
Feat : Fonction recursive qui copie le exit_status dans la final_str;
WIP

about expand
Fix : Expansion terminer (sans avoir tester encore), creation de la token->data avec la string final sans 
les quotes ou les dollars ect
Fix : si probleme dans get_value return NULL
Feat : creation d'un expansion.h meilleure structuration du projet (probleme en compilant Inclusion circulaire)
WIP

gerer get_value et mettre strdup car strlen(getvalue)

Fix : Expansion completement terminer avec les tests
Fix : return ft_strdup("") dans getvalue pour le strlen 
Feat : Utils/free_exp
Retour si erreur return (exemple malloc failed etc) a continuer
WIP